# Транзакции в PostgreSQL

## 📖 Содержание

- [Что такое транзакция](#что-такое-транзакция)
- [ACID](#acid)
- [Уровни изоляции](#уровни-изоляции)
- [Установка уровня изоляции](#установка-уровня-изоляции)

---

## Что такое транзакция

**Транзакция** — это группа SQL-операций, которая выполняется как **одно целое**.

Если хотя бы один запрос из транзакции завершается с ошибкой — **ни одно изменение не будет сохранено в БД**.

### Основные команды

| Команда | Описание |
|---------|----------|
| `BEGIN` | Начать транзакцию |
| `COMMIT` | Подтвердить и сохранить изменения |
| `ROLLBACK` | Откатить все изменения |

### Пример транзакции

```sql
BEGIN;

INSERT INTO accounts (user_id, balance) VALUES (1, 1000);

-- Снимаем деньги с одного счёта
UPDATE accounts SET balance = balance - 500 WHERE user_id = 1;

-- Переводим на другой счёт
UPDATE accounts SET balance = balance + 500 WHERE user_id = 999;
-- ❌ Ошибка! user_id = 999 не существует

-- Если ошибка — откатываем:
ROLLBACK;

-- Если всё ок — сохраняем:
COMMIT;
```

**Как это работает:**
- Если возникла ошибка → транзакция блокируется
- `ROLLBACK` отменяет транзакцию, снимает блокировку, данные **не изменяются**
- Если всё прошло успешно → `COMMIT` сохраняет данные

---

## ACID

**ACID** — набор принципов, гарантирующих **надёжность и целостность данных** в транзакциях.

| Принцип |          Название             | Описание |
|---------|-------------------------------|----------|
| **A**   | Atomicity (Атомарность) | Транзакция либо выполнится **полностью**, либо **не выполнится совсем** |
| **C**   | Consistency (Согласованность) | После транзакции данные остаются корректными: соблюдены все правила, ограничения, связи |
| **I**   | Isolation (Изоляция) | Параллельные транзакции не мешают друг другу — каждая видит данные так, будто выполняется одна |
| **D**   | Durability (Долговечность) | После `COMMIT` данные сохраняются даже при сбоях сервера |

---

## Уровни изоляции

**Уровень изоляции** определяет, какие данные одна транзакция может видеть из другой **незавершённой** транзакции.

### Проблемы параллельного доступа

| Проблема | Описание |
|----------|----------|
| **Dirty Read** | Чтение данных, которые ещё не закоммичены (могут быть откачены) |
| **Non-Repeatable Read** | Повторное чтение той же строки возвращает другие данные |
| **Phantom Read** | Повторный `SELECT` возвращает новые строки, добавленные другой транзакцией |

---

### 1️⃣ Read Uncommitted

Можно читать даже **незакоммиченные** данные.

```sql
-- Транзакция A
BEGIN;
SELECT balance FROM accounts WHERE id = 1;  -- 1000

-- Транзакция B (параллельно) снимает 500, но НЕ коммитит

SELECT balance FROM accounts WHERE id = 1;  -- 500 (грязное чтение!)
-- Если B откатится — данные A некорректны
COMMIT;
```

**Проблемы:** Dirty Read, Non-Repeatable Read, Phantom Read

> ⚠️ **Особенность PostgreSQL:** уровень `Read Uncommitted` работает как `Read Committed`. PostgreSQL **не позволяет** читать незакоммиченные данные даже на этом уровне.

---

### 2️⃣ Read Committed ⭐

Видны только **закоммиченные** данные. **Уровень по умолчанию в PostgreSQL.**

```sql
-- Транзакция A
BEGIN;
SELECT balance FROM accounts WHERE id = 1;  -- 1000

-- Транзакция B снимает 500 и КОММИТИТ

SELECT balance FROM accounts WHERE id = 1;  -- 500 (данные изменились!)
COMMIT;
```

**Проблемы:** Non-Repeatable Read, Phantom Read  
**Решает:** Dirty Read

---

### 3️⃣ Repeatable Read

Все `SELECT` в транзакции видят **один и тот же снимок данных** (snapshot).

```sql
-- Транзакция A
BEGIN;
SELECT balance FROM accounts WHERE id = 1;  -- 1000

-- Транзакция B снимает 500 и коммитит

SELECT balance FROM accounts WHERE id = 1;  -- всё ещё 1000!
COMMIT;
```

**Проблемы:** Phantom Read (в некоторых БД, но **не в PostgreSQL**)  
**Решает:** Dirty Read, Non-Repeatable Read

> 💡 **В PostgreSQL** уровень `Repeatable Read` также защищает от Phantom Read благодаря механизму MVCC.

---

### 4️⃣ Serializable

**Полная изоляция** — транзакции выполняются так, будто идут **последовательно, одна за другой**.

```sql
-- Транзакция A
BEGIN ISOLATION LEVEL SERIALIZABLE;
SELECT SUM(balance) FROM accounts;  -- 10000

-- Транзакция B пытается добавить новый счёт
-- PostgreSQL обнаружит конфликт и откатит одну из транзакций

COMMIT;
```

**Проблемы:** Нет  
**Решает:** Dirty Read, Non-Repeatable Read, Phantom Read

> ⚠️ Самый строгий и **медленный** уровень. Может приводить к откатам транзакций при конфликтах.

---

### Сравнительная таблица

| Уровень | Dirty Read | Non-Repeatable Read | Phantom Read |
|---------|:----------:|:-------------------:|:------------:|
| Read Uncommitted | ✅* | ✅ | ✅ |
| Read Committed | ❌ | ✅ | ✅ |
| Repeatable Read | ❌ | ❌ | ❌** |
| Serializable | ❌ | ❌ | ❌ |

✅ — проблема возможна  
❌ — проблема предотвращена  
\* В PostgreSQL работает как Read Committed  
\** В PostgreSQL Phantom Read также предотвращён

---

## Установка уровня изоляции

### Для одной транзакции

```sql
-- Способ 1: в BEGIN
BEGIN ISOLATION LEVEL REPEATABLE READ;
-- ... запросы ...
COMMIT;

-- Способ 2: отдельной командой
BEGIN;
SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;
-- ... запросы ...
COMMIT;
```

### Для всей сессии

```sql
SET SESSION CHARACTERISTICS AS TRANSACTION ISOLATION LEVEL REPEATABLE READ;
```

### Проверить текущий уровень

```sql
SHOW transaction_isolation;
```

---

## Когда использовать

| Уровень | Когда применять |
|---------|-----------------|
| **Read Committed** | По умолчанию, подходит для большинства операций |
| **Repeatable Read** | Критичные отчёты, когда данные не должны меняться во время транзакции |
| **Serializable** | Финансовые операции, когда важна абсолютная точность и согласованность |
| **Read Uncommitted** | Почти не используется в PostgreSQL (работает как Read Committed) |

---

## SAVEPOINT — точки сохранения

Позволяют откатить **часть транзакции**, а не всю целиком.

```sql
BEGIN;

INSERT INTO users (email) VALUES ('a@mail.com');
SAVEPOINT sp1;

INSERT INTO users (email) VALUES ('b@mail.com');
-- Что-то пошло не так...
ROLLBACK TO sp1;  -- откатит только до savepoint

INSERT INTO users (email) VALUES ('c@mail.com');
COMMIT;  -- сохранит a@mail.com и c@mail.com
```

---

> 💡 **Совет:** Держите транзакции **короткими**. Долгие транзакции блокируют ресурсы и могут замедлить работу других пользователей.

